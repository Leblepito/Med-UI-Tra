[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r 02_backend/requirements.txt"]

[start]
cmd = "cd 02_backend && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
